[project]
name = "netflix"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "pandas>=3.0.2",
    "plotly>=6.0.0",
    "streamlit>=1.57.0",
]

[project.scripts]
netflix = "netflix:main"

[build-system]
requires = ["uv_build>=0.8.17,<0.9.0"]
build-backend = "uv_build"
